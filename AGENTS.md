# Agent instructions

This repository is the source of truth for Kevin's machine-learning-to-research study roadmap.

Before editing:
1. Read `ROADMAP.md` and the relevant supporting files.
2. Verify the current GitHub branch/tip and do not overwrite unexplained work.
3. Check AgentCtl status for concurrent work.

Read-only inspection does not require a lease. Any repository mutation requires the exact AgentCtl resource `repo:Kiwinokoto/ml-research-roadmap`. Do not invent broader host/service/device scopes for this documentation-only project.

If the project is archived in AgentCtl, reactivate it through the controlled admin broker before starting a write session; do not delete and recreate the project identity. Keep the existing roadmap structure unless the requested change genuinely requires restructuring.
